// app/admin/rodadas/page.tsx — gerenciar rodadas.
// Botão "fechar rodada": chama /api/sync/ratings e /api/scoring/compute.
export default function RoundsAdmin() {
  return <main><h1>Rodadas</h1><p>TODO: fechar rodada (puxar notas + calcular).</p></main>;
}
